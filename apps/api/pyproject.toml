[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "agent-studio-api"
version = "0.1.0"
description = "AI Innovation Agent Studio API"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["app/tests"]
